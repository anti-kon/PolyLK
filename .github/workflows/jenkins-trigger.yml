name: trigger jenkins job

on:
    push:
        branches:
            - master
            - develop

jobs:
    launch_jenkins_job:
        name: trigger jenkins job
        runs-on: ubuntu-latest
        steps:
            - name: trigger single Job
              uses: sbitio/trigger-jenkins-job@master
              with:
                  JENKINS_USER: ${{ Jenkins.JENKINS_USER }}
                  JENKINS_TOKEN: ${{ Jenkins.JENKINS_TOKEN }}
                  JENKINS_HOST: ${{ Jenkins.JENKINS_HOST }}
                  JENKINS_JOB: ${{ Jenkins.JENKINS_JOB }}
